name: CI/CD Pipeline

on:
 push:
  branches:
   - main

jobs:
 build:
  runs-on: ubuntu-latest

  steps:
   - name: Checkout repository
     uses: actions/checkout@v3

   - name: Build step
     run: echo "Сборка проекта завершена успешно"

   - name: Test step
     run: echo "Тестирование выполнено успешно"

   - name: Deploy step
     run: echo "Деплой приложения выполнен"